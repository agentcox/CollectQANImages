extends layout

block content
  h1= title
  p Welcome to #{title}

  each item in items
    div(class='img')
      a(href='#{item.filename}')
        img(src='#{item.filename}' width='300' height='225')
      div(class = 'desc')
        p #{item.date}